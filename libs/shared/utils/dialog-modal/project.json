{
  "name": "dialog-modal",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/utils/dialog-modal/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["utils"],
  "targets": {
    "targets": {
      "test": {
        "executor": "@nx/vitest:test",
        "outputs": [
          "{options.reportsDirectory}"
        ],
        "options": {
          "reportsDirectory": "../../../../coverage/libs/shared/utils/dialog-modal"
        }
      },
      "lint": {
        "executor": "@nx/eslint:lint"
      }
    }
  }
}
